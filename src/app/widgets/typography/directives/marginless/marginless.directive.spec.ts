import { MarginlessDirective } from './marginless.directive';

describe('MarginlessDirective', () => {
  it('should create an instance', () => {
    const directive = new MarginlessDirective();
    expect(directive).toBeTruthy();
  });
});
